<% @topics.each do |topic| %>
   <div class="post-preview text-center">
    <h2 class="post-title">
      <%= link_to topic.title, topic_path(topic) %>
    </h2>
     <p class="post-meta">Number of blogs posts: <%= topic.blogs.count %></p>
     <hr>
  </div>
<% end %>